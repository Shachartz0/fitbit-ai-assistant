name: fitbit-ai
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - langchain>=0.2.0
      - langgraph>=0.0.40
      - langchain-anthropic>=0.1.8
      - anthropic>=0.25.0
      - python-dotenv
      - ipykernel
      - jupyter
